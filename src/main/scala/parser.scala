package org.gnode.lib.parse

import org.gnode.lib.neo._
import org.gnode.lib.util._
import org.gnode.lib.api._

import net.liftweb.json._
import dispatch._

object NEOParser {

  def make(data: String): Option[NEObject] = {}
  def apply(data: String) = make data

}
